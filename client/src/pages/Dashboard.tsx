import Header from "../components/Header";
const Dashboard = () => {
    return (
        <main className="w-full">
            <Header />
            
        </main>
    );
};

export default Dashboard;
